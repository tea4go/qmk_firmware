# Keychron V5 Max QMK 固件编译与部署文档

## 目录
- [环境信息](#环境信息)
- [编译环境配置](#编译环境配置)
- [问题与解决方案](#问题与解决方案)
- [编译步骤](#编译步骤)
- [生成的固件](#生成的固件)
- [固件刷写](#固件刷写)
- [附录](#附录)

---

## 环境信息

### 系统环境
- **操作系统**: Windows 11 Pro 10.0.28000
- **Shell**: bash (MSYS2 环境)
- **项目路径**: `D:\MyWork\AiCode\qmk_firmware`
- **QMK MSYS 路径**: `C:\DevDisk\DevSoft\QMK_MSYS`

### 目标键盘
- **型号**: Keychron V5 Max
- **布局**: ANSI 98 键 + 旋转编码器
- **MCU**: STM32F401
- **平台**: ChibiOS
- **RGB 驱动**: SNLED27351 (SPI)

### 工具链版本
- **ARM GCC**: 13.3.0
- **QMK 版本**: 0.14.29
- **ChibiOS**: lib/chibios (Git 子模块)
- **LUFA**: lib/lufa (Git 子模块)

---

## 编译环境配置

### 1. QMK MSYS 安装
QMK MSYS 已安装在：`C:\DevDisk\DevSoft\QMK_MSYS`

### 2. Git 子模块初始化
```bash
# 初始化 ChibiOS 子模块
git submodule update --init --recursive lib/chibios lib/chibios-contrib

# 初始化 LUFA 子模块
git submodule update --init --recursive lib/lufa
```

### 3. ARM 工具链安装
```bash
# 使用 MSYS2 pacman 安装 ARM 交叉编译工具链
pacman -Sy mingw-w64-x86_64-arm-none-eabi-toolchain --noconfirm
```

### 4. 关键文件创建

#### 4.1 编译脚本 `compile_with_qmk.bat`
```batch
@echo off
:: Batch script to compile in QMK MSYS environment
cd /d D:\MyWork\AiCode\qmk_firmware
set HOME=C:\Users\tony
set MSYSTEM=MINGW64
set MSYS2_PATH_TYPE=inherit
C:\DevDisk\DevSoft\QMK_MSYS\usr\bin\bash.exe --login -c "cd /d/MyWork/AiCode/qmk_firmware && qmk compile -kb keychron/v5_max/ansi_encoder -km default"
pause
```

#### 4.2 ChibiOS printf 兼容文件 `platforms/chibios/printf.c`
```c
/*
Copyright 2024 QMK

This program is free software: you can redistribute it and/or modify
it under the terms of the GNU General Public License as published by
the Free Software Foundation, either version 2 of the License, or
(at your option) any later version.
*/

// ChibiOS uses standard library printf
// This file is included for compatibility with build system
```

#### 4.3 VIA keymap rules.mk 修改
文件路径: `keyboards/keychron/v5_max/ansi_encoder/keymaps/via/rules.mk`
```makefile
VIA_ENABLE = yes
SEND_STRING_ENABLE = no
```

---

## 问题与解决方案

### 问题 1: HOME 目录识别失败
**错误信息**:
```
RuntimeError: Could not determine home directory.
```

**解决方案**:
在 `compile_with_qmk.bat` 中设置 `HOME` 环境变量：
```batch
set HOME=C:\Users\tony
```

### 问题 2: Git 子模块未初始化
**错误信息**:
```
lib/chibios-contrib/os/common/startup/ARMCMx/compilers/GCC/mk/startup_stm32f4xx.mk: No such file
```

**解决方案**:
```bash
git submodule update --init --recursive lib/chibios lib/chibios-contrib
git submodule update --init --recursive lib/lufa
```

### 问题 3: ARM GCC 工具链缺失
**错误信息**:
```
arm-none-eabi-gcc: command not found
```

**解决方案**:
```bash
pacman -Sy mingw-w64-x86_64-arm-none-eabi-toolchain --noconfirm
```

### 问题 4: 平台未定义
**错误信息**:
```
Platform not defined (builddefs/build_keyboard.mk:226)
```

**解决方案**:
QMK 自动生成的 `info_rules.mk` 已包含：
```makefile
PLATFORM_KEY = chibios
MCU = STM32F401
```

### 问题 5: printf.o 链接错误
**错误信息**:
```
cannot find .build/obj_keychron_v5_max_ansi_encoder_via/printf.o: No error
```

**解决方案**:
创建 `platforms/chibios/printf.c` 占位文件（ChibiOS 使用标准库 printf）

### 问题 6: RGB 矩阵驱动未定义
**错误信息**:
```
RGB_MATRIX_DRIVER= is not a valid matrix type
```

**解决方案**:
QMK 从 `info.json` 自动提取并生成：
```makefile
RGB_MATRIX_DRIVER = snled27351_spi
```

### 问题 7: 矩阵和 USB 配置缺失
**错误信息**:
```
'MATRIX_ROWS' undeclared here
'PRODUCT_ID' undeclared
```

**解决方案**:
QMK 从 `info.json` 自动生成 `info_config.h`：
```c
#define MATRIX_ROWS 6
#define MATRIX_COLS 19
#define MATRIX_ROW_PINS { C12, D2, B3, B4, B5, B6 }
#define MATRIX_COL_PINS { C6, C7, C8, A14, A15, C10, C11, C13, C14, C15, C0, C1, C2, C3, A0, A1, A2, A3, C9 }
#define PRODUCT "Keychron V5 Max"
#define MANUFACTURER "Keychron"
#define VENDOR_ID 0x3434
#define PRODUCT_ID 0x0950
#define DEVICE_VER 0x0100
```

### 问题 8: 编码器配置缺失
**解决方案**:
QMK 自动生成编码器配置：
```c
#ifndef ENCODERS_PAD_A
#define ENCODERS_PAD_A { B15 }
#endif
#ifndef ENCODERS_PAD_B
#define ENCODERS_PAD_B { B14 }
#endif
```

---

## 编译步骤

### 方法 1: 使用批处理脚本（推荐）

1. 运行编译脚本：
```batch
compile_with_qmk.bat
```

2. 等待编译完成，固件文件将自动复制到项目根目录

### 方法 2: 手动编译

#### 编译 default keymap
```bash
cd D:\MyWork\AiCode\qmk_firmware
qmk compile -kb keychron/v5_max/ansi_encoder -km default
```

#### 编译 VIA keymap
```bash
qmk compile -kb keychron/v5_max/ansi_encoder -km via
```

### 编译输出示例
```
Compiling: keyboards/keychron/v5_max/v5_max.c                                [OK]
Compiling: keyboards/keychron/common/wireless/wireless.c                     [OK]
Compiling: quantum/quantum.c                                                 [OK]
...
Linking: .build/keychron_v5_max_ansi_encoder_default.elf                     [OK]
Creating binary load file for flashing: .build/keychron_v5_max_ansi_encoder_default.bin [OK]

Size after:
   text	   data	    bss	    dec	    hex	filename
      0	  65394	      0	  65394	   ff72	keychron_v5_max_ansi_encoder_default.bin

Copying keychron_v5_max_ansi_encoder_default.bin to qmk_firmware folder      [OK]
```

---

## 生成的固件

### 固件文件列表

| 文件名 | 大小 | 说明 |
|--------|------|------|
| `keychron_v5_max_ansi_encoder_default.bin` | 96KB | 默认键映射固件 |
| `keychron_v5_max_ansi_encoder_via.bin` | 99KB | VIA 支持固件 |

### 固件位置
固件文件位于项目根目录：
```
D:\MyWork\AiCode\qmk_firmware\keychron_v5_max_ansi_encoder_default.bin
D:\MyWork\AiCode\qmk_firmware\keychron_v5_max_ansi_encoder_via.bin
```

### 固件功能对比

#### Default 固件
- ✅ 标准 QMK 功能
- ✅ 4 层键映射（MAC_BASE, MAC_FN, WIN_BASE, WIN_FN）
- ✅ RGB 矩阵灯效
- ✅ 旋转编码器支持
- ✅ 无线功能（LKBT51）
- ❌ 不支持 VIA 实时配置

#### VIA 固件
- ✅ 所有 Default 固件功能
- ✅ **VIA 软件实时配置支持**
- ✅ 通过 VIA 软件自定义按键
- ✅ 实时修改灯效
- ✅ 无需重新编译即可更改配置

---

## 固件刷写

### 工具选择

#### 选项 1: QMK Toolbox（推荐）
1. 下载并安装 [QMK Toolbox](https://github.com/qmk/qmk_toolbox/releases)
2. 打开 QMK Toolbox
3. 选择对应的 `.bin` 固件文件
4. 将键盘置于 DFU 模式：
   - 按住键盘背面的 Reset 按钮
   - 或使用组合键（通常是 Fn + Esc）
5. 点击 "Flash" 按钮刷写固件

#### 选项 2: Keychron 官方刷写工具
1. 访问 [Keychron 官网](https://www.keychron.com/pages/firmware-and-software)
2. 下载 Keychron Launcher 或对应的刷写工具
3. 按照官方说明进行刷写

#### 选项 3: dfu-util（命令行）
```bash
# STM32 DFU 模式刷写
dfu-util -a 0 -d 0483:df11 -s 0x08000000 -D keychron_v5_max_ansi_encoder_default.bin
```

### 刷写注意事项

⚠️ **刷写前请注意**：
1. 确保电池电量充足（无线版本）或连接 USB 线缆
2. 备份当前固件（如果需要）
3. 不要在刷写过程中断开连接
4. 刷写完成后键盘会自动重启

### 固件验证

刷写完成后验证功能：
- [ ] 所有按键是否正常响应
- [ ] RGB 灯效是否工作
- [ ] 旋转编码器是否正常
- [ ] 无线连接是否正常（如果适用）
- [ ] VIA 软件是否能识别键盘（VIA 固件）

---

## 附录

### A. 键盘规格

**物理规格**：
- 布局：98 键 ANSI
- 编码器：1 个旋转编码器
- 矩阵：6 行 × 19 列
- MCU：STM32F401（ARM Cortex-M4）
- 闪存：足够空间（当前使用 ~65-99KB）

**功能特性**：
- 机械轴热插拔
- RGB 矩阵背光（SNLED27351 驱动）
- 蓝牙 5.1 无线连接（LKBT51 模块）
- 2.4GHz 无线连接
- USB Type-C 有线连接
- VIA 支持（使用 VIA 固件）

### B. 键映射层级

#### Layer 0: MAC_BASE
- 标准 Mac 布局
- 多媒体控制键
- 旋转编码器：音量控制

#### Layer 1: MAC_FN
- F1-F12 功能键
- RGB 控制
- 蓝牙配对（BT_HST1/2/3）
- 电池电量显示（BAT_LVL）
- 旋转编码器：RGB 亮度

#### Layer 2: WIN_BASE
- 标准 Windows 布局
- F1-F12 功能键
- 旋转编码器：音量控制

#### Layer 3: WIN_FN
- 系统控制（任务视图、文件管理器）
- RGB 控制
- 蓝牙配对
- 电池电量显示
- 旋转编码器：RGB 亮度

### C. RGB 矩阵配置

**驱动**：SNLED27351 (SPI 接口)
- SPI_SCK_PIN: A5
- SPI_MISO_PIN: A6
- SPI_MOSI_PIN: A7

**可用灯效**：
- 纯色
- 呼吸灯
- 彩虹流动
- 按键反应
- 等多种预设效果

### D. 无线功能

**LKBT51 模块**：
- 蓝牙 5.1
- 2.4GHz 无线
- 最多 3 个蓝牙设备配对
- 低功耗模式支持
- 电池电量监控

**配对方式**：
- Fn + 1/2/3：蓝牙通道切换
- Fn + P2P4G：2.4GHz 模式切换

### E. 编译配置文件路径

```
keyboards/keychron/v5_max/
├── info.json                          # 键盘主配置
├── config.h                           # 全局配置
├── rules.mk                           # 编译规则
├── ansi_encoder/
│   ├── info.json                      # ANSI 编码器版本配置
│   ├── config.h                       # 特定配置
│   └── keymaps/
│       ├── default/
│       │   └── keymap.c               # 默认键映射
│       └── via/
│           ├── keymap.c               # VIA 键映射
│           └── rules.mk               # VIA 编译规则
└── firmware/
    ├── keychron_v5_max_ansi_encoder_via.bin    # 预编译固件
    └── keychron_v5_max_iso_encoder_via.bin
```

### F. 常用命令参考

```bash
# 查看键盘信息
qmk info -kb keychron/v5_max/ansi_encoder

# 列出所有键映射
qmk list-keymaps -kb keychron/v5_max/ansi_encoder

# 清理编译缓存
qmk clean

# 编译并刷写（进入 DFU 模式后）
qmk flash -kb keychron/v5_max/ansi_encoder -km default
```

### G. 问题排查

**编译失败**：
1. 检查 Git 子模块是否完整初始化
2. 确认 ARM 工具链已正确安装
3. 清理编译缓存：`qmk clean`
4. 检查 `platforms/chibios/printf.c` 是否存在

**刷写失败**：
1. 确认键盘已进入 DFU 模式
2. 检查 USB 线缆连接
3. 尝试更换 USB 端口
4. 使用管理员权限运行刷写工具

**VIA 无法识别**：
1. 确认使用的是 VIA 固件
2. 下载最新版本 [VIA](https://usevia.app/)
3. 在 VIA 设置中启用"Show Design Tab"
4. 如需要，手动导入键盘 JSON 定义

### H. 参考资源

- [QMK 官方文档](https://docs.qmk.fm/)
- [Keychron 官网](https://www.keychron.com/)
- [VIA 配置工具](https://usevia.app/)
- [QMK Toolbox](https://github.com/qmk/qmk_toolbox)
- [QMK Discord 社区](https://discord.gg/qmk)

---

## 编译完成时间

**初始编译**: 2026-03-31
**文档版本**: 1.0
**编译环境**: Windows 11 + QMK MSYS + ARM GCC 13.3.0

---

**注意**: 此文档记录了 Keychron V5 Max ANSI 编码器版本的完整编译过程。如果遇到问题，请参考本文档的"问题与解决方案"章节，或访问 QMK 官方文档获取更多帮助。
